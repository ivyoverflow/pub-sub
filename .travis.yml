language: go

go:
  - 1.15.x
  - tip

install:
  - curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin v1.35.2

script:
  - chmod +x server/scripts/linter.sh
  - chmod +x listener/scripts/linter.sh
  - chmod +x server/scripts/test.sh
